[build]
  command = "npm run build"
  publish = ".next"

# Official Netlify runtime for Next.js (handles SSR, image optimization, routing)
[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
